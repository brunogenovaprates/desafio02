const app = require("./app");

app.listen(3333, () => { //Escuta a porta que a aplicação irá subir
    console.log(`Status UP 🚀🚀🚀`); //printa no terminal retorno quando aplicação sobe
});